READ_INT:
    MOV BX, 0
READ_LOOP:
    MOV AH, 01H
    INT 21H
    CMP AL, 0DH
    JE WRITE_NEWLINE
    MOV CH, 0
    MOV CL, AL
    MOV AX, BX
    MOV BX, 10
    MUL BX
    MOV BX, CX
    SUB BX, 30H
    ADD BX, AX
    JMP READ_LOOP
